
## File 6: `render.yaml`

```yaml
services:
  - type: web
    name: confession-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python confession_bot.py
    envVars:
      - key: BOT_TOKEN
        value: your_bot_token_here
      - key: ADMIN_CHAT_ID  
        value: your_user_id_here
      - key: CHANNEL_ID
        value: your_channel_id_here
      - key: BOT_USERNAME
        value: your_bot_username
